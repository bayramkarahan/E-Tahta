#ifndef BUTTONCONFIG_H
#define BUTTONCONFIG_H

#include <mainwindow.h>

class ButtonConfig : public QMainWindow
{
    Q_OBJECT
public:
    explicit ButtonConfig(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // BUTTONCONFIG_H
